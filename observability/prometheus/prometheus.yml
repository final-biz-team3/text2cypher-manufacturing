global:
  scrape_interval: 15s
rule_files: ["/etc/prometheus/rules.yml"]
scrape_configs:
  - job_name: itda-backend
    metrics_path: /internal/metrics
    http_headers:
      X-Metrics-Token:
        # Local observability profile token. Production injects a secret-backed file.
        values: ["development-only-metrics-token"]
    static_configs:
      # 두 Compose 프로젝트가 공유하는 내부 네트워크에서 backend 서비스로 접근한다.
      - targets: ["backend:8000"]
  - job_name: prometheus
    scrape_interval: 30s
    static_configs: [{targets: ["localhost:9090"]}]
  - job_name: loki
    scrape_interval: 30s
    static_configs: [{targets: ["loki:3100"]}]
  - job_name: alloy
    scrape_interval: 30s
    static_configs: [{targets: ["alloy:12345"]}]
